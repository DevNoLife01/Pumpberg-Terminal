[project]
name = "crypto-terminal-backend"
version = "0.1.0"
description = "Bloomberg terminal — FastAPI backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.110.0",
    "websockets>=12.0",
    "pandas>=2.1.0",
    "numpy>=1.26.0",
]

[tool.setuptools]
packages = []
